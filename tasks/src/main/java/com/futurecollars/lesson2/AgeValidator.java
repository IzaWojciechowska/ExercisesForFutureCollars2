package com.futurecollars.lesson2;

public class AgeValidator {
    public static boolean isAdult(int age) {
        if (age >= 18) {
            return true;
        } else
            return false;
    }
}
